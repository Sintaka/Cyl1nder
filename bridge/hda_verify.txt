{
 "instance_count": 1,
 "instances": [
  {
   "path": "/obj/geo1/Cyl1nder1",
   "serial": "C1-msm6dsp7-ob6t"
  }
 ],
 "buttons": {
  "force_cook": "node = hou.pwd()\nfor n in node.children():\n    if n.type().name() == \"python\":\n        n.cook(force=True)\n",
  "open_web": "import webbrowser\nimport threading\nimport urllib.request\nnode = hou.pwd()\nbase = node.parm('web_url').eval().rstrip('/')",
  "cyl1nder_regenerate": "node = hou.pwd()\nimport cyl1nder_bridge as cb\nnode.parm(\"cyl1nder_serial\").set(cb.generate_serial())\n"
 }
}