{
  "name": "cyl1nder-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@antv/x6": "^2.18.1",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "@types/three": "^0.180.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}